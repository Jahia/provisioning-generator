query getChildrenByPath($path: String!, $childrenTypes: [String] = [], $workspace: Workspace = EDIT) {
    jcr(workspace: $workspace) {
        nodeByPath(path: $path) {
            children(typesFilter: {types: $childrenTypes}) {
                nodes {
                    name
                }
            }
        }
    }
}
